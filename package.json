{
  "name": "UDPChat",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "client": "nodemon --watch 'src/' --exec 'ts-node src/client.ts' -e ts",
    "server": "nodemon --watch 'src/' --exec 'ts-node src/server.ts' -e ts"
  },
  "devDependencies": {
    "@types/node": "^16.11.10",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
